query OgAccount($request: AccountRequest!) {
  account(request: $request) {
    address
    owner
    username(request: { autoResolve: true }) {
      ...Username
    }
    metadata {
      name
      bio
      picture
    }
  }
}
